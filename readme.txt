Extract the zip and open a command prompt and run below commands
1)python -m venv explainer_env
2).\explainer_env\Scripts\activate.bat
3)pip install -r requirements.txt
4)python -m streamlit run app.py